apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/ssathi/spring-service11:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/ssathi/spring-service11:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: spring-service11
        chartPath: charts/spring-service11
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/ssathi/spring-service11'
          image.tag: '{{.DIGEST_HEX}}'
